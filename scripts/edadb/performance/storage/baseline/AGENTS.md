# Frozen baseline

This directory is an approved experimental baseline. Do not edit, rename,
delete, regenerate, or update files here without the user's explicit approval
for the proposed change. Suggestions alone do not authorize implementation.

Keep subsequent SQLite/EDADB control experiments in ../sqlite-vs-edadb/.
Do not silently replace historical results with new measurements. Changes to
shared benchmark code or configuration that affect reproduction of this
baseline must also be proposed to the user before implementation.

This is an agent workflow rule, not an operating-system write restriction.
